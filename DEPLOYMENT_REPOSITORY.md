# Deployment repository

The production Render service is connected to:

`https://github.com/gurpreetbumrah1971/manishas-kitchen-new`

Use the Git remote named `render-new` for all deployment pushes. Do not use
`origin` (`manishas-kitchen-php`) for production deployments unless explicitly
asked.
